import {environment} from "../../environments/environment";

export class EndPoints {
  static SUBJECT = environment.REST_CORE + "/subject";
  static SEASON = environment.REST_CORE + "/season";
  static VIDEO = environment.REST_CORE + "/video";
  static USER =  environment.REST_CORE + "/user";


}
